---
interface Props {
    title: string;
    subtitle: string;
    href: string;
    coverColor?: string;
    pattern?: "circles" | "grid" | "waves" | "dots";
    fontStyle?: "serif" | "sans" | "display" | "handwritten";
    layoutStyle?: "classic" | "minimal" | "modern";
    orientation?: "vertical" | "horizontal";
}

const {
    title,
    subtitle,
    href,
    coverColor = "#3b82f6",
    pattern = "circles",
    fontStyle = "serif",
    layoutStyle = "classic",
    orientation = "vertical",
} = Astro.props;
---

<a href={href} class="book-card-link" data-orientation={orientation}>
    <div
        class="book-card"
        data-layout={layoutStyle}
        data-orientation={orientation}
    >
        <div
            class="book-cover"
            style={`background: linear-gradient(135deg, ${coverColor}, ${coverColor}dd)`}
            data-pattern={pattern}
            data-font={fontStyle}
        >
            <div class="book-spine"></div>
            <div class="book-content">
                <h3 class="book-title">{title}</h3>
                <!-- <p class="book-subtitle">{subtitle}</p> -->
                <div class="book-badge" style={`border-color: ${coverColor}40`}>
                    TEXTBOOK
                </div>
            </div>
            <div class="book-decor"></div>
        </div>
        <div class="book-pages"></div>
    </div>
</a>

<style>
    .book-card-link {
        text-decoration: none;
        display: block;
        width: 100%;
        /* Ensure the card doesn't get too small or too huge */
        min-width: 120px; /* Reduced from 140px for mobile */
        max-width: 200px;
        margin: 0 auto;
        perspective: 1000px;
        /* Container Query Setup */
        container-type: inline-size;
        container-name: book-card;
    }

    .book-card {
        position: relative;
        width: 100%;
        aspect-ratio: 2/3; /* Book aspect ratio */
        transition: transform 0.4s ease;
        transform-style: preserve-3d;
        transform: rotateY(-12deg) rotateX(4deg); /* Slightly less dramatic for stability */
        box-shadow: 8px 8px 25px rgba(0, 0, 0, 0.3);
    }

    /* Mobile Responsive Tweak: Flatten angle to save space */
    @media (max-width: 600px) {
        .book-card {
            transform: rotateY(-4deg) rotateX(1deg);
        }
    }

    .book-card-link:hover .book-card {
        transform: rotateY(0deg) scale(1.02) translateY(-5px);
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
    }

    .book-cover {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        border-radius: 2px 3px 3px 2px;
        z-index: 10;
        /* Responsive padding linked directly to container width */
        padding: 6cqw; /* Reduced padding for more title space */
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        color: white;
        overflow: hidden;
        /* Cloth Texture Base */
        background-color: var(--cover-color, #333);
    }

    /* Cloth Texture Overlay */
    .book-cover::before {
        content: "";
        position: absolute;
        inset: 0;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='1.5' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.15'/%3E%3C/svg%3E");
        opacity: 0.5;
        mix-blend-mode: overlay;
        pointer-events: none;
        z-index: 1;
    }

    /* Spine Hinge Indentation */
    .book-cover::after {
        content: "";
        position: absolute;
        top: 0;
        bottom: 0;
        left: 8px; /* Slightly tighter spine */
        width: 1.5px;
        background: rgba(0, 0, 0, 0.4);
        box-shadow: 0.5px 0 1px rgba(255, 255, 255, 0.1);
        z-index: 2;
    }

    .book-spine {
        position: absolute;
        left: 0;
        top: 0;
        bottom: 0;
        width: 12px; /* Tighter spine */
        background: linear-gradient(
            90deg,
            rgba(255, 255, 255, 0.2) 0%,
            rgba(0, 0, 0, 0.1) 20%,
            rgba(0, 0, 0, 0.2) 80%,
            rgba(0, 0, 0, 0.4) 100%
        );
        transform: rotateY(-90deg) translateX(-12px);
        transform-origin: left;
        z-index: 5;
        background-color: var(--cover-color, #333);
    }

    .book-content {
        position: relative;
        z-index: 30;
        margin-left: 0.3rem;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        border: 1.5px solid rgba(197, 160, 89, 0.3); /* Softer gold border */
        padding: 4cqw;
        justify-content: center;
    }

    /* Gold Foil Title */
    .book-title {
        font-family: "Cinzel", serif;
        font-size: 11cqw !important; /* Slightly larger share */
        font-weight: 700;
        line-height: 1.1;
        margin-bottom: 0.5rem;

        color: #e5c17d; /* Gold base */
        background: linear-gradient(
            to bottom right,
            #fceeb5 0%,
            #e5c17d 50%,
            #9e7f3c 100%
        );
        -webkit-background-clip: text;
        background-clip: text;
        /* Transparent text for gradient, but fallback needed for shadow */
        color: transparent;

        filter: drop-shadow(0px 1px 1px rgba(0, 0, 0, 0.3));
        letter-spacing: 0.02em;
        word-break: break-word;
    }

    /* Fallback for text-shadow on transparent text (often tricky), using simpler gold */
    @supports (-webkit-background-clip: text) {
        .book-title {
            text-shadow: none;
            filter: drop-shadow(0px 1px 1px rgba(0, 0, 0, 0.5));
        }
    }

    .book-badge {
        font-family: "Oswald", sans-serif;
        font-size: 4.5cqw;
        letter-spacing: 0.15em;
        color: rgba(197, 160, 89, 0.7); /* Gold */
        margin-top: auto;
        text-transform: uppercase;
        border-top: 1px solid rgba(197, 160, 89, 0.2);
        padding-top: 0.3rem;
        width: 100%;
    }

    /* Patterns */
    .book-decor {
        display: none; /* Simplify for vintage look */
    }

    /* Pages effect - old yellowed paper */
    .book-pages {
        position: absolute;
        top: 2px;
        right: -8px; /* Tighter pages */
        width: 10px;
        height: calc(100% - 4px);
        background: #f4e4bc; /* Yellowed paper */
        border-radius: 0 2px 2px 0;
        box-shadow:
            inset 0 0 3px rgba(0, 0, 0, 0.2),
            3px 3px 10px rgba(0, 0, 0, 0.2);
        z-index: 0;
        transform: translateZ(-1px); /* Push behind cover slightly */
    }

    /* Page striations */
    .book-pages::before {
        content: "";
        position: absolute;
        top: 0;
        bottom: 0;
        left: 0;
        width: 100%;
        background: repeating-linear-gradient(
            90deg,
            transparent,
            transparent 1px,
            rgba(0, 0, 0, 0.08) 2px
        );
    }
</style>
